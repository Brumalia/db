#!/usr/bin/env bash

MYSQL_USER="${MYSQL_USER:-brumalia}"
MYSQL_PASSWORD="${MYSQL_PASSWORD:-secret12345}"
MYSQL_DATABASE="${MYSQL_DATABASE:-brumalia}"

cat $1 | mysql -u${MYSQL_USER} -p${MYSQL_PASSWORD} -D ${MYSQL_DATABASE}